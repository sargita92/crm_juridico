---
feature: F04-especialistas-crud
agent: arquiteto
version: v1
created_at: 2026-04-05
reason: design tecnico inicial
---

# Design Tecnico — F04 CRUD de Especialistas (Admin)

## 1. Dominio

### 1.1 Entidade Specialist (nova)

```go
// internal/specialist/domain/specialist.go

type SpecialistStatus string

const (
    SpecialistStatusActive   SpecialistStatus = "active"
    SpecialistStatusInactive SpecialistStatus = "inactive"
)

const MaxPromptLength = 10000
const MaxDescriptionLength = 500

type Specialist struct {
    ID          string
    Name        string
    Description string
    Prompt      string
    Status      SpecialistStatus
    CreatedAt   time.Time
    UpdatedAt   time.Time
}

func NewSpecialist(id, name, description, prompt string) (*Specialist, error) {
    if name == "" {
        return nil, ErrSpecialistNameRequired
    }
    if prompt == "" {
        return nil, ErrSpecialistPromptRequired
    }
    if len(prompt) > MaxPromptLength {
        return nil, ErrPromptTooLong
    }
    if len(description) > MaxDescriptionLength {
        return nil, ErrDescriptionTooLong
    }
    now := time.Now()
    return &Specialist{
        ID:          id,
        Name:        name,
        Description: description,
        Prompt:      prompt,
        Status:      SpecialistStatusActive,
        CreatedAt:   now,
        UpdatedAt:   now,
    }, nil
}

func (s *Specialist) Update(name, description, prompt string) error {
    if name == "" {
        return ErrSpecialistNameRequired
    }
    if prompt == "" {
        return ErrSpecialistPromptRequired
    }
    if len(prompt) > MaxPromptLength {
        return ErrPromptTooLong
    }
    if len(description) > MaxDescriptionLength {
        return ErrDescriptionTooLong
    }
    s.Name = name
    s.Description = description
    s.Prompt = prompt
    s.UpdatedAt = time.Now()
    return nil
}

func (s *Specialist) UpdatePrompt(prompt string) error {
    if prompt == "" {
        return ErrSpecialistPromptRequired
    }
    if len(prompt) > MaxPromptLength {
        return ErrPromptTooLong
    }
    s.Prompt = prompt
    s.UpdatedAt = time.Now()
    return nil
}

func (s *Specialist) Deactivate() error {
    if s.Status == SpecialistStatusInactive {
        return ErrSpecialistAlreadyInactive
    }
    s.Status = SpecialistStatusInactive
    s.UpdatedAt = time.Now()
    return nil
}

func (s *Specialist) Activate() error {
    if s.Status == SpecialistStatusActive {
        return ErrSpecialistAlreadyActive
    }
    s.Status = SpecialistStatusActive
    s.UpdatedAt = time.Now()
    return nil
}

func (s *Specialist) IsActive() bool {
    return s.Status == SpecialistStatusActive
}
```

### 1.2 Entidade SpecialistTenant (nova)

```go
// internal/specialist/domain/specialist_tenant.go

type SpecialistTenant struct {
    SpecialistID string
    TenantID     string
    CreatedAt    time.Time
}
```

### 1.3 Erros de dominio

```go
// internal/specialist/domain/errors.go

var (
    ErrSpecialistNotFound        = errors.New("specialist not found")
    ErrSpecialistNameRequired    = errors.New("specialist name is required")
    ErrSpecialistPromptRequired  = errors.New("specialist prompt is required")
    ErrPromptTooLong             = errors.New("prompt exceeds maximum length")
    ErrDescriptionTooLong        = errors.New("description exceeds maximum length")
    ErrSpecialistAlreadyInactive = errors.New("specialist is already inactive")
    ErrSpecialistAlreadyActive   = errors.New("specialist is already active")
    ErrSpecialistInactive        = errors.New("specialist is inactive")
    ErrTenantAlreadyAssociated   = errors.New("tenant is already associated")
    ErrTenantNotAssociated       = errors.New("tenant is not associated")
)
```

### 1.4 Value Objects — Filtros

```go
// internal/specialist/domain/specialist.go

type SpecialistFilter struct {
    Search string            // busca por nome
    Status SpecialistStatus  // filtro por status (vazio = todos)
    Page   int
    Limit  int
}

type SpecialistList struct {
    Specialists []Specialist
    Total       int64
    Page        int
    Limit       int
}
```

## 2. Interfaces de Repositorio

### 2.1 SpecialistRepository (novo)

```go
// internal/specialist/domain/repository.go

type SpecialistRepository interface {
    Create(ctx context.Context, specialist *Specialist) error
    FindByID(ctx context.Context, id string) (*Specialist, error)
    Update(ctx context.Context, specialist *Specialist) error
    FindWithFilter(ctx context.Context, filter SpecialistFilter) (*SpecialistList, error)
}
```

### 2.2 SpecialistTenantRepository (novo)

```go
// internal/specialist/domain/repository.go

type SpecialistTenantRepository interface {
    Associate(ctx context.Context, specialistID, tenantID string) error
    Dissociate(ctx context.Context, specialistID, tenantID string) error
    FindTenantIDsBySpecialistID(ctx context.Context, specialistID string) ([]string, error)
    FindSpecialistIDsByTenantID(ctx context.Context, tenantID string) ([]string, error)
    Exists(ctx context.Context, specialistID, tenantID string) (bool, error)
}
```

## 3. Casos de Uso

### 3.1 CreateSpecialistUseCase

```go
// internal/specialist/application/create_specialist.go

type CreateSpecialistInput struct {
    Name        string
    Description string
    Prompt      string
}

type CreateSpecialistOutput struct {
    ID          string
    Name        string
    Description string
    Status      string
}

func (uc *CreateSpecialistUseCase) Execute(ctx context.Context, input CreateSpecialistInput) (*CreateSpecialistOutput, error)
// Fluxo: gera UUID → NewSpecialist → repo.Create → retorna output
```

### 3.2 ListSpecialistsUseCase

```go
// internal/specialist/application/list_specialists.go

type ListSpecialistsInput struct {
    Search string
    Status string
    Page   int
    Limit  int
}

type SpecialistItem struct {
    ID           string
    Name         string
    Description  string
    Status       string
    TenantCount  int
    CreatedAt    time.Time
}

type ListSpecialistsOutput struct {
    Specialists []SpecialistItem
    Total       int64
    Page        int
    Limit       int
}

func (uc *ListSpecialistsUseCase) Execute(ctx context.Context, input ListSpecialistsInput) (*ListSpecialistsOutput, error)
// Fluxo: monta SpecialistFilter → repo.FindWithFilter → para cada specialist, conta tenants → retorna output
```

### 3.3 GetSpecialistUseCase

```go
// internal/specialist/application/get_specialist.go

type GetSpecialistOutput struct {
    ID          string
    Name        string
    Description string
    Prompt      string
    Status      string
    CreatedAt   time.Time
    UpdatedAt   time.Time
}

func (uc *GetSpecialistUseCase) Execute(ctx context.Context, id string) (*GetSpecialistOutput, error)
// Fluxo: repo.FindByID → retorna output
```

### 3.4 UpdateSpecialistUseCase

```go
// internal/specialist/application/update_specialist.go

type UpdateSpecialistInput struct {
    ID          string
    Name        string
    Description string
    Prompt      string
}

func (uc *UpdateSpecialistUseCase) Execute(ctx context.Context, input UpdateSpecialistInput) (*GetSpecialistOutput, error)
// Fluxo: repo.FindByID → specialist.Update() → repo.Update → retorna output
```

### 3.5 DeactivateSpecialistUseCase

```go
// internal/specialist/application/deactivate_specialist.go

func (uc *DeactivateSpecialistUseCase) Execute(ctx context.Context, id string) error
// Fluxo: repo.FindByID → specialist.Deactivate() → repo.Update
```

### 3.6 ActivateSpecialistUseCase

```go
// internal/specialist/application/activate_specialist.go

func (uc *ActivateSpecialistUseCase) Execute(ctx context.Context, id string) error
// Fluxo: repo.FindByID → specialist.Activate() → repo.Update
```

### 3.7 AssociateSpecialistTenantUseCase

```go
// internal/specialist/application/associate_tenant.go

type AssociateTenantInput struct {
    SpecialistID string
    TenantIDs    []string
}

func (uc *AssociateTenantUseCase) Execute(ctx context.Context, input AssociateTenantInput) error
// Fluxo:
//   specialistRepo.FindByID → valida que esta ativo
//   para cada tenantID:
//     tenantRepo.FindByID → valida que existe e esta ativo
//     specialistTenantRepo.Exists → valida que nao esta duplicado
//     specialistTenantRepo.Associate
```

### 3.8 DissociateSpecialistTenantUseCase

```go
// internal/specialist/application/dissociate_tenant.go

type DissociateTenantInput struct {
    SpecialistID string
    TenantID     string
}

func (uc *DissociateTenantUseCase) Execute(ctx context.Context, input DissociateTenantInput) error
// Fluxo: specialistRepo.FindByID → specialistTenantRepo.Exists → specialistTenantRepo.Dissociate
```

### 3.9 ListSpecialistTenantsUseCase

```go
// internal/specialist/application/list_specialist_tenants.go

type SpecialistTenantItem struct {
    TenantID string
    Name     string
    Type     string
    Status   string
}

func (uc *ListSpecialistTenantsUseCase) Execute(ctx context.Context, specialistID string) ([]SpecialistTenantItem, error)
// Fluxo: specialistRepo.FindByID → specialistTenantRepo.FindTenantIDsBySpecialistID → tenantRepo.FindByIDs → retorna
```

### 3.10 ListAvailableTenantsUseCase

```go
// internal/specialist/application/list_available_tenants.go

type AvailableTenantItem struct {
    TenantID string
    Name     string
    Type     string
    Document string
}

func (uc *ListAvailableTenantsUseCase) Execute(ctx context.Context, specialistID string, search string) ([]AvailableTenantItem, error)
// Fluxo:
//   specialistTenantRepo.FindTenantIDsBySpecialistID → obtem IDs ja associados
//   tenantRepo.FindWithFilter(status=active, search) → filtra os ja associados → retorna
```

## 4. Contratos HTTP

Todas as rotas sob `/admin/specialists` protegidas por `authMw` + `adminMw`.

### Rotas

| Metodo | Rota | Handler | Request | Response |
|--------|------|---------|---------|----------|
| GET | /admin/specialists | RenderList | query: search, status, page | HTML (listagem) |
| GET | /admin/specialists/new | RenderCreateForm | — | HTML (formulario) |
| POST | /admin/specialists | HandleCreate | form: name, description, prompt | HTML (form c/ erro) ou HX-Redirect |
| GET | /admin/specialists/:id | RenderDetail | — | HTML (detalhe) |
| GET | /admin/specialists/:id/edit | RenderEditForm | — | HTML (formulario) |
| PUT | /admin/specialists/:id | HandleUpdate | form: name, description, prompt | HTML (form c/ erro) ou HX-Redirect |
| POST | /admin/specialists/:id/deactivate | HandleDeactivate | — | HTML (detalhe atualizado) |
| POST | /admin/specialists/:id/activate | HandleActivate | — | HTML (detalhe atualizado) |
| GET | /admin/specialists/:id/tenants | HandleListTenants | — | HTML (secao tenants) |
| GET | /admin/specialists/:id/tenants/available | HandleListAvailable | query: search | HTML (lista tenants disponiveis) |
| POST | /admin/specialists/:id/tenants | HandleAssociateTenants | form: tenant_ids[] | HTML (secao tenants atualizada) |
| DELETE | /admin/specialists/:id/tenants/:tenant_id | HandleDissociateTenant | — | HTML (secao tenants atualizada) |

### Respostas HTMX

- Formularios: retornam fragmento HTML do formulario (com erros inline) ou `HX-Redirect`
- Listagem: filtros retornam fragmento da tabela (`#specialists-table`)
- Detalhe: acoes de ativar/desativar retornam fragmento do detalhe (`#specialist-detail`)
- Tenants: associar/desassociar retornam fragmento da secao (`#tenants-section`)

## 5. Migrations

### 000006_create_specialists_table

```sql
-- UP
CREATE TABLE specialists (
    id CHAR(36) PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description VARCHAR(500) NOT NULL DEFAULT '',
    prompt TEXT NOT NULL,
    status ENUM('active', 'inactive') NOT NULL DEFAULT 'active',
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_specialists_status (status),
    INDEX idx_specialists_name (name)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- DOWN
DROP TABLE IF EXISTS specialists;
```

### 000007_create_specialist_tenants_table

```sql
-- UP
CREATE TABLE specialist_tenants (
    specialist_id CHAR(36) NOT NULL,
    tenant_id CHAR(36) NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (specialist_id, tenant_id),
    CONSTRAINT fk_specialist_tenants_specialist FOREIGN KEY (specialist_id)
        REFERENCES specialists(id) ON DELETE CASCADE,
    CONSTRAINT fk_specialist_tenants_tenant FOREIGN KEY (tenant_id)
        REFERENCES tenants(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;

-- DOWN
DROP TABLE IF EXISTS specialist_tenants;
```

## 6. Templates

```text
web/templates/
  specialist/
    list.html           # pagina de listagem
    table.html          # fragmento tabela (para HTMX)
    detail.html         # pagina de detalhe
    form.html           # formulario criar/editar
    tenants_section.html    # secao de tenants associados (lazy-load)
    tenants_modal.html      # modal de associacao de tenants
    deactivate_modal.html   # modal de confirmacao de desativacao
```

## 7. Composicao de Dependencias

Adicionar ao `main.go`:

```go
// Specialist repositories
specialistRepo := specialistinfra.NewGormSpecialistRepository(db)
specialistTenantRepo := specialistinfra.NewGormSpecialistTenantRepository(db)

// Specialist use cases
createSpecialistUC := specialistapp.NewCreateSpecialistUseCase(specialistRepo)
listSpecialistsUC := specialistapp.NewListSpecialistsUseCase(specialistRepo, specialistTenantRepo)
getSpecialistUC := specialistapp.NewGetSpecialistUseCase(specialistRepo)
updateSpecialistUC := specialistapp.NewUpdateSpecialistUseCase(specialistRepo)
deactivateSpecialistUC := specialistapp.NewDeactivateSpecialistUseCase(specialistRepo)
activateSpecialistUC := specialistapp.NewActivateSpecialistUseCase(specialistRepo)
associateTenantUC := specialistapp.NewAssociateTenantUseCase(specialistRepo, tenantRepo, specialistTenantRepo)
dissociateTenantUC := specialistapp.NewDissociateTenantUseCase(specialistRepo, specialistTenantRepo)
listSpecialistTenantsUC := specialistapp.NewListSpecialistTenantsUseCase(specialistRepo, specialistTenantRepo, tenantRepo)
listAvailableTenantsUC := specialistapp.NewListAvailableTenantsUseCase(specialistTenantRepo, tenantRepo)

// Specialist handler
specialistHandler := specialisthttp.NewHandler(
    createSpecialistUC, listSpecialistsUC, getSpecialistUC,
    updateSpecialistUC, deactivateSpecialistUC, activateSpecialistUC,
    associateTenantUC, dissociateTenantUC,
    listSpecialistTenantsUC, listAvailableTenantsUC,
)

// Registro de rotas
specialistHandler.RegisterRoutes(router, authMw, adminMw)
```

## 8. Impacto em Modulos Existentes

| Modulo | Impacto |
|--------|---------|
| `internal/specialist/` | Modulo novo (domain, application, infrastructure, interfaces/http) |
| `internal/tenant/domain/repository.go` | Nenhuma alteracao — ja possui FindByIDs e FindWithFilter |
| `cmd/api/main.go` | Wire-up de repositorios, use cases e handler do specialist |
| `web/templates/` | Novos templates specialist/ |
| `web/templates/partials/sidebar.html` | Adicionar link "Especialistas" |
| `web/templates/tenant/detail.html` | Futuramente: exibir especialistas associados (fora do escopo minimo) |

## 9. Steps de Implementacao

### Step 1: Dominio — entidades, erros e interfaces de repositorio

**Escopo:** Criar entidade Specialist com construtor, metodos Update, UpdatePrompt, Deactivate, Activate. Criar entidade SpecialistTenant. Definir erros de dominio. Definir interfaces SpecialistRepository e SpecialistTenantRepository. Definir SpecialistFilter e SpecialistList.

**Arquivos:**
- `internal/specialist/domain/specialist.go` (novo)
- `internal/specialist/domain/specialist_tenant.go` (novo)
- `internal/specialist/domain/errors.go` (novo)
- `internal/specialist/domain/repository.go` (novo)
- `internal/specialist/domain/specialist_test.go` (novo)

**Validacao:** testes unitarios do dominio passando.

---

### Step 2: Infraestrutura — migrations + repositorios GORM

**Escopo:** Criar migrations para tabelas specialists e specialist_tenants. Implementar GormSpecialistRepository e GormSpecialistTenantRepository.

**Arquivos:**
- `migrations/000006_create_specialists_table.up.sql` (novo)
- `migrations/000006_create_specialists_table.down.sql` (novo)
- `migrations/000007_create_specialist_tenants_table.up.sql` (novo)
- `migrations/000007_create_specialist_tenants_table.down.sql` (novo)
- `internal/specialist/infrastructure/gorm_specialist_repository.go` (novo)
- `internal/specialist/infrastructure/gorm_specialist_tenant_repository.go` (novo)
- `internal/specialist/infrastructure/gorm_specialist_repository_test.go` (novo)
- `internal/specialist/infrastructure/gorm_specialist_tenant_repository_test.go` (novo)

**Validacao:** testes de integracao do repositorio passando (testcontainers-go).

---

### Step 3: Application — casos de uso CRUD (Create, List, Get, Update)

**Escopo:** Implementar CreateSpecialistUseCase, ListSpecialistsUseCase, GetSpecialistUseCase, UpdateSpecialistUseCase.

**Arquivos:**
- `internal/specialist/application/create_specialist.go` (novo)
- `internal/specialist/application/list_specialists.go` (novo)
- `internal/specialist/application/get_specialist.go` (novo)
- `internal/specialist/application/update_specialist.go` (novo)
- `internal/specialist/application/create_specialist_test.go` (novo)
- `internal/specialist/application/list_specialists_test.go` (novo)
- `internal/specialist/application/get_specialist_test.go` (novo)
- `internal/specialist/application/update_specialist_test.go` (novo)

**Validacao:** testes unitarios dos use cases passando.

---

### Step 4: Application — casos de uso Deactivate/Activate

**Escopo:** Implementar DeactivateSpecialistUseCase e ActivateSpecialistUseCase.

**Arquivos:**
- `internal/specialist/application/deactivate_specialist.go` (novo)
- `internal/specialist/application/activate_specialist.go` (novo)
- `internal/specialist/application/deactivate_specialist_test.go` (novo)
- `internal/specialist/application/activate_specialist_test.go` (novo)

**Validacao:** testes unitarios passando.

---

### Step 5: Application — casos de uso de associacao com tenants

**Escopo:** Implementar AssociateTenantUseCase, DissociateTenantUseCase, ListSpecialistTenantsUseCase, ListAvailableTenantsUseCase.

**Arquivos:**
- `internal/specialist/application/associate_tenant.go` (novo)
- `internal/specialist/application/dissociate_tenant.go` (novo)
- `internal/specialist/application/list_specialist_tenants.go` (novo)
- `internal/specialist/application/list_available_tenants.go` (novo)
- `internal/specialist/application/associate_tenant_test.go` (novo)
- `internal/specialist/application/dissociate_tenant_test.go` (novo)
- `internal/specialist/application/list_specialist_tenants_test.go` (novo)
- `internal/specialist/application/list_available_tenants_test.go` (novo)

**Validacao:** testes unitarios passando.

---

### Step 6: Handler HTTP + composicao no main.go

**Escopo:** Criar handler com todas as rotas. Registrar no main.go com wire-up completo.

**Arquivos:**
- `internal/specialist/interfaces/http/handler.go` (novo)
- `internal/specialist/interfaces/http/handler_test.go` (novo)
- `cmd/api/main.go` (alterar)

**Validacao:** testes de integracao do handler passando. Build ok.

---

### Step 7: Templates HTMX — listagem + detalhe + formularios

**Escopo:** Criar templates de listagem, detalhe, formulario criar/editar. Atualizar sidebar com link de Especialistas.

**Arquivos:**
- `web/templates/specialist/list.html` (novo)
- `web/templates/specialist/table.html` (novo)
- `web/templates/specialist/detail.html` (novo)
- `web/templates/specialist/form.html` (novo)
- `web/templates/specialist/deactivate_modal.html` (novo)
- `web/templates/partials/sidebar.html` (alterar)

**Validacao:** telas renderizam corretamente. Filtros, paginacao e CRUD funcionam via HTMX.

---

### Step 8: Templates HTMX — associacao de tenants

**Escopo:** Criar templates para secao de tenants associados e modal de associacao.

**Arquivos:**
- `web/templates/specialist/tenants_section.html` (novo)
- `web/templates/specialist/tenants_modal.html` (novo)

**Validacao:** associacao e desassociacao funcionam. Lazy-load da secao ok.

---

### Step 9: Testes OWASP + arquivo .http

**Escopo:** Adicionar testes de acesso nao autorizado (401/403), anti-injection. Criar arquivo .http para testes manuais.

**Arquivos:**
- `internal/specialist/interfaces/http/handler_test.go` (alterar — cenarios OWASP)
- `rest/03-admin-specialists.http` (novo)

**Validacao:** todos os testes passando. Cobertura >= 80%.
