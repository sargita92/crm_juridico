---
feature: F07-funis-kanban
agent: arquiteto
version: v1
created_at: 2026-04-06
reason: design tecnico inicial
---

# Design Tecnico — F07 Funis de Vendas (Kanban)

## Visao geral

F07 cria o modulo `funnel` com dominio proprio (Funnel, Column, Lead, LeadMovement) dentro de `internal/funnel/`. O modulo integra com o modulo `whatsapp` (F06) para criacao automatica de leads ao receber primeira mensagem. Segue a mesma estrutura DDD + Clean Architecture do projeto.

---

## 1. Dominio

### 1.1 Entidade Funnel

```go
// internal/funnel/domain/funnel.go

type Funnel struct {
    ID          string
    TenantID    string
    Name        string
    Description string
    Active      bool
    IsDefault   bool
    CreatedAt   time.Time
    UpdatedAt   time.Time
}

const MaxFunnelNameLength = 255

func NewFunnel(id, tenantID, name, description string) (*Funnel, error) {
    if tenantID == "" {
        return nil, ErrTenantIDRequired
    }
    if name == "" {
        return nil, ErrFunnelNameRequired
    }
    if len(name) > MaxFunnelNameLength {
        return nil, ErrFunnelNameTooLong
    }
    now := time.Now()
    return &Funnel{
        ID: id, TenantID: tenantID, Name: name,
        Description: description, Active: true, IsDefault: false,
        CreatedAt: now, UpdatedAt: now,
    }, nil
}

func (f *Funnel) Update(name, description string) error
func (f *Funnel) Activate()
func (f *Funnel) Deactivate()
func (f *Funnel) SetDefault()
```

### 1.2 Entidade Column

```go
// internal/funnel/domain/column.go

type ColumnType string

const (
    ColumnTypeEntry        ColumnType = "entry"
    ColumnTypeIntermediate ColumnType = "intermediate"
    ColumnTypeWon          ColumnType = "won"
    ColumnTypeLost         ColumnType = "lost"
)

type Column struct {
    ID         string
    FunnelID   string
    Name       string
    OrderIndex int
    Type       ColumnType
    Color      string    // hex color ex: "#22c55e"
    CreatedAt  time.Time
    UpdatedAt  time.Time
}

const MaxColumnNameLength = 100
const MaxColumnsPerFunnel = 15

func NewColumn(id, funnelID, name string, orderIndex int, colType ColumnType, color string) (*Column, error)
func (c *Column) Update(name string, colType ColumnType, color string) error
```

### 1.3 Entidade Lead

```go
// internal/funnel/domain/lead.go

type LeadStatus string

const (
    LeadStatusOpen   LeadStatus = "open"
    LeadStatusWon    LeadStatus = "won"
    LeadStatusLost   LeadStatus = "lost"
)

type Lead struct {
    ID             string
    TenantID       string
    FunnelID       string
    ColumnID       string
    ContactID      string
    ConversationID string
    Score          int
    Status         LeadStatus
    ColumnEnteredAt time.Time
    CreatedAt      time.Time
    UpdatedAt      time.Time
}

func NewLead(id, tenantID, funnelID, columnID, contactID, conversationID string) (*Lead, error)
func (l *Lead) MoveTo(columnID string, colType ColumnType)
func (l *Lead) UpdateScore(score int)
```

### 1.4 Entidade LeadMovement (historico)

```go
// internal/funnel/domain/lead_movement.go

type LeadMovement struct {
    ID           string
    LeadID       string
    FromColumnID string    // vazio na criacao
    ToColumnID   string
    MovedAt      time.Time
}

func NewLeadMovement(id, leadID, fromColumnID, toColumnID string) *LeadMovement
```

### 1.5 Colunas padrao

```go
// internal/funnel/domain/defaults.go

var DefaultColumns = []struct {
    Name  string
    Type  ColumnType
    Color string
}{
    {"Novo", ColumnTypeEntry, "#22c55e"},
    {"Em Atendimento", ColumnTypeIntermediate, "#3b82f6"},
    {"Qualificado", ColumnTypeIntermediate, "#eab308"},
    {"Ganho", ColumnTypeWon, "#16a34a"},
    {"Perdido", ColumnTypeLost, "#ef4444"},
}
```

### 1.6 Erros de dominio

```go
// internal/funnel/domain/errors.go

var (
    ErrTenantIDRequired       = errors.New("tenant ID is required")
    ErrFunnelNotFound         = errors.New("funnel not found")
    ErrFunnelNameRequired     = errors.New("funnel name is required")
    ErrFunnelNameTooLong      = errors.New("funnel name exceeds maximum length")
    ErrFunnelInactive         = errors.New("funnel is inactive")

    ErrColumnNotFound         = errors.New("column not found")
    ErrColumnNameRequired     = errors.New("column name is required")
    ErrColumnNameTooLong      = errors.New("column name exceeds maximum length")
    ErrColumnTypeInvalid      = errors.New("column type is invalid")
    ErrColumnHasLeads         = errors.New("column has leads and cannot be removed")
    ErrColumnLimitReached     = errors.New("maximum number of columns reached")

    ErrLeadNotFound           = errors.New("lead not found")
    ErrLeadAlreadyExists      = errors.New("lead already exists for this contact")
    ErrContactIDRequired      = errors.New("contact ID is required")
    ErrConversationIDRequired = errors.New("conversation ID is required")
    ErrFunnelIDRequired       = errors.New("funnel ID is required")
    ErrColumnIDRequired       = errors.New("column ID is required")
)
```

### 1.7 Repositorios

```go
// internal/funnel/domain/repository.go

type FunnelRepository interface {
    Create(ctx context.Context, funnel *Funnel) error
    FindByID(ctx context.Context, id string) (*Funnel, error)
    Update(ctx context.Context, funnel *Funnel) error
    FindByTenantID(ctx context.Context, tenantID string) ([]Funnel, error)
    FindDefaultByTenantID(ctx context.Context, tenantID string) (*Funnel, error)
}

type ColumnRepository interface {
    Create(ctx context.Context, col *Column) error
    FindByID(ctx context.Context, id string) (*Column, error)
    Update(ctx context.Context, col *Column) error
    Delete(ctx context.Context, id string) error
    FindByFunnelID(ctx context.Context, funnelID string) ([]Column, error)
    FindEntryByFunnelID(ctx context.Context, funnelID string) (*Column, error)
    CountByFunnelID(ctx context.Context, funnelID string) (int, error)
    GetMaxOrderIndex(ctx context.Context, funnelID string) (int, error)
    SwapOrder(ctx context.Context, col1ID string, order1 int, col2ID string, order2 int) error
}

type LeadRepository interface {
    Create(ctx context.Context, lead *Lead) error
    FindByID(ctx context.Context, id string) (*Lead, error)
    Update(ctx context.Context, lead *Lead) error
    FindByContactAndTenant(ctx context.Context, tenantID, contactID string) (*Lead, error)
    FindByColumnID(ctx context.Context, columnID string) ([]Lead, error)
    FindByFunnelID(ctx context.Context, funnelID string, filter LeadFilter) (*LeadList, error)
    CountByColumnID(ctx context.Context, columnID string) (int, error)
}

type LeadMovementRepository interface {
    Create(ctx context.Context, movement *LeadMovement) error
    FindByLeadID(ctx context.Context, leadID string) ([]LeadMovement, error)
}

type LeadFilter struct {
    Search   string
    ColumnID string
    Page     int
    Limit    int
}

type LeadList struct {
    Leads []LeadWithContact
    Total int64
    Page  int
    Limit int
}

type LeadWithContact struct {
    Lead        Lead
    ContactName  string
    ContactPhone string
    ColumnName   string
    ColumnColor  string
}
```

---

## 2. Casos de Uso

### 2.1 Funis

```go
// internal/funnel/application/

// CreateFunnelUseCase
// Fluxo: cria funil → cria 5 colunas padrao → se primeiro funil, marca como default
func (uc *CreateFunnelUseCase) Execute(ctx, input CreateFunnelInput) (*FunnelOutput, error)

// ListFunnelsUseCase
func (uc *ListFunnelsUseCase) Execute(ctx, tenantID string) ([]FunnelItem, error)

// GetFunnelUseCase (inclui colunas)
func (uc *GetFunnelUseCase) Execute(ctx, tenantID, funnelID string) (*FunnelDetailOutput, error)

// UpdateFunnelUseCase
func (uc *UpdateFunnelUseCase) Execute(ctx, input UpdateFunnelInput) (*FunnelOutput, error)

// ToggleFunnelUseCase (ativar/desativar)
func (uc *ToggleFunnelUseCase) Execute(ctx, tenantID, funnelID string) error
```

### 2.2 Colunas

```go
// CreateColumnUseCase
// Fluxo: valida limite (15) → GetMaxOrderIndex → NewColumn(order+1) → Create
func (uc *CreateColumnUseCase) Execute(ctx, input CreateColumnInput) (*ColumnOutput, error)

// DeleteColumnUseCase
// Fluxo: CountLeadsByColumnID → se > 0 erro → Delete → reorder
func (uc *DeleteColumnUseCase) Execute(ctx, tenantID, funnelID, columnID string) error

// MoveColumnUseCase (up/down)
func (uc *MoveColumnUseCase) Execute(ctx, tenantID, funnelID, columnID, direction string) error
```

### 2.3 Leads

```go
// CreateLeadUseCase (chamado pelo whatsapp module)
// Fluxo: FindDefaultFunnel → FindEntryColumn → NewLead → Create → CreateMovement
func (uc *CreateLeadUseCase) Execute(ctx, input CreateLeadInput) (*Lead, error)

// MoveLeadUseCase
// Fluxo: FindLead (valida tenant) → FindColumn destino → lead.MoveTo → Update → CreateMovement
func (uc *MoveLeadUseCase) Execute(ctx, input MoveLeadInput) error

// GetLeadDetailUseCase
func (uc *GetLeadDetailUseCase) Execute(ctx, tenantID, leadID string) (*LeadDetailOutput, error)
```

### 2.4 Kanban

```go
// GetKanbanUseCase
// Fluxo: FindFunnel → FindColumns → para cada coluna: FindLeads com contacto info
func (uc *GetKanbanUseCase) Execute(ctx, input GetKanbanInput) (*KanbanOutput, error)

type GetKanbanInput struct {
    TenantID string
    FunnelID string  // se vazio, usa default
    Search   string
}

type KanbanOutput struct {
    Funnel  FunnelItem
    Funnels []FunnelItem  // para o seletor
    Columns []KanbanColumn
}

type KanbanColumn struct {
    ID        string
    Name      string
    Color     string
    Type      string
    LeadCount int
    Leads     []KanbanLead
}

type KanbanLead struct {
    ID             string
    ContactName    string
    ContactPhone   string
    Score          int
    ColumnEnteredAt time.Time
}
```

---

## 3. Contratos HTTP

### 3.1 Paginas (HTML completo)

| Metodo | Rota | Descricao | Auth |
|--------|------|-----------|------|
| GET | /tenant/leads | Kanban principal | auth + tenant |
| GET | /tenant/leads/funnels | Lista de funis | auth + tenant |
| GET | /tenant/leads/funnels/:id | Detalhe do funil (colunas) | auth + tenant |

### 3.2 Fragmentos HTMX

| Metodo | Rota | Descricao | Auth |
|--------|------|-----------|------|
| GET | /tenant/leads/kanban | Fragmento do kanban (colunas + cards) | auth + tenant |
| GET | /tenant/leads/:id | Modal detalhe do lead | auth + tenant |
| POST | /tenant/leads/:id/move | Mover lead (column_id, funnel_id) | auth + tenant |
| GET | /tenant/leads/funnels/:id/columns | Fragmento lista de colunas | auth + tenant |
| POST | /tenant/leads/funnels | Criar funil | auth + tenant |
| PUT | /tenant/leads/funnels/:id | Editar funil | auth + tenant |
| POST | /tenant/leads/funnels/:id/toggle | Ativar/desativar funil | auth + tenant |
| POST | /tenant/leads/funnels/:id/columns | Criar coluna | auth + tenant |
| DELETE | /tenant/leads/funnels/:id/columns/:cid | Remover coluna | auth + tenant |
| POST | /tenant/leads/funnels/:id/columns/:cid/move-up | Mover coluna cima | auth + tenant |
| POST | /tenant/leads/funnels/:id/columns/:cid/move-down | Mover coluna baixo | auth + tenant |

---

## 4. Migrations

### 000019_create_funnels_table

```sql
CREATE TABLE IF NOT EXISTS funnels (
    id CHAR(36) PRIMARY KEY,
    tenant_id CHAR(36) NOT NULL,
    name VARCHAR(255) NOT NULL,
    description TEXT DEFAULT '',
    active BOOLEAN NOT NULL DEFAULT TRUE,
    is_default BOOLEAN NOT NULL DEFAULT FALSE,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_funnels_tenant_id (tenant_id),
    CONSTRAINT fk_funnels_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 000020_create_funnel_columns_table

```sql
CREATE TABLE IF NOT EXISTS funnel_columns (
    id CHAR(36) PRIMARY KEY,
    funnel_id CHAR(36) NOT NULL,
    name VARCHAR(100) NOT NULL,
    order_index INT NOT NULL DEFAULT 0,
    type ENUM('entry', 'intermediate', 'won', 'lost') NOT NULL DEFAULT 'intermediate',
    color VARCHAR(7) NOT NULL DEFAULT '#3b82f6',
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_columns_funnel_id (funnel_id),
    CONSTRAINT fk_columns_funnel FOREIGN KEY (funnel_id) REFERENCES funnels(id) ON DELETE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 000021_create_leads_table

```sql
CREATE TABLE IF NOT EXISTS leads (
    id CHAR(36) PRIMARY KEY,
    tenant_id CHAR(36) NOT NULL,
    funnel_id CHAR(36) NOT NULL,
    column_id CHAR(36) NOT NULL,
    contact_id CHAR(36) NOT NULL,
    conversation_id CHAR(36) NOT NULL,
    score INT NOT NULL DEFAULT 0,
    status ENUM('open', 'won', 'lost') NOT NULL DEFAULT 'open',
    column_entered_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    INDEX idx_leads_tenant_id (tenant_id),
    INDEX idx_leads_funnel_id (funnel_id),
    INDEX idx_leads_column_id (column_id),
    INDEX idx_leads_contact_id (contact_id),
    UNIQUE INDEX idx_leads_tenant_contact (tenant_id, contact_id),
    CONSTRAINT fk_leads_tenant FOREIGN KEY (tenant_id) REFERENCES tenants(id),
    CONSTRAINT fk_leads_funnel FOREIGN KEY (funnel_id) REFERENCES funnels(id),
    CONSTRAINT fk_leads_column FOREIGN KEY (column_id) REFERENCES funnel_columns(id),
    CONSTRAINT fk_leads_contact FOREIGN KEY (contact_id) REFERENCES contacts(id),
    CONSTRAINT fk_leads_conversation FOREIGN KEY (conversation_id) REFERENCES conversations(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

### 000022_create_lead_movements_table

```sql
CREATE TABLE IF NOT EXISTS lead_movements (
    id CHAR(36) PRIMARY KEY,
    lead_id CHAR(36) NOT NULL,
    from_column_id CHAR(36) DEFAULT NULL,
    to_column_id CHAR(36) NOT NULL,
    moved_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_movements_lead_id (lead_id),
    CONSTRAINT fk_movements_lead FOREIGN KEY (lead_id) REFERENCES leads(id) ON DELETE CASCADE,
    CONSTRAINT fk_movements_from FOREIGN KEY (from_column_id) REFERENCES funnel_columns(id),
    CONSTRAINT fk_movements_to FOREIGN KEY (to_column_id) REFERENCES funnel_columns(id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci;
```

---

## 5. Integracao com modulo WhatsApp

O modulo `whatsapp` precisa chamar `CreateLeadUseCase` do modulo `funnel` quando uma primeira mensagem chega. A integracao e feita via interface no dominio do whatsapp:

```go
// internal/whatsapp/domain/lead_creator.go (nova interface)

type LeadCreator interface {
    CreateFromConversation(ctx context.Context, tenantID, contactID, conversationID string) error
}
```

O `ReceiveMessageUseCase` do whatsapp recebe um `LeadCreator` opcional e chama ao criar nova conversa. O modulo `funnel` implementa essa interface.

**Wire-up no main.go:**
```go
funnelMod := funnel.NewModule(db, log)
// Passar o lead creator do funnel para o whatsapp module
whatsappMod := whatsapp.NewModule(db, whatsmeowProvider, log, funnelMod.LeadCreator())
```

---

## 6. Composicao de dependencias

### Module

```go
// internal/funnel/module.go

func NewModule(db *gorm.DB, log *zap.Logger) *Module {
    funnelRepo := infrastructure.NewGormFunnelRepository(db)
    columnRepo := infrastructure.NewGormColumnRepository(db)
    leadRepo := infrastructure.NewGormLeadRepository(db)
    movementRepo := infrastructure.NewGormLeadMovementRepository(db)

    // Use cases
    createFunnelUC := application.NewCreateFunnelUseCase(funnelRepo, columnRepo)
    // ... demais use cases

    handler := http.NewHandler(...)
    leadCreator := application.NewCreateLeadUseCase(funnelRepo, columnRepo, leadRepo, movementRepo)

    return &Module{handler: handler, leadCreator: leadCreator}
}

func (m *Module) LeadCreator() domain.LeadCreator { return m.leadCreator }
```

---

## 7. Impacto em modulos existentes

| Modulo | Impacto |
|--------|---------|
| whatsapp/domain | Nova interface `LeadCreator` |
| whatsapp/application | `ReceiveMessageUseCase` recebe `LeadCreator` opcional |
| whatsapp/module | Recebe `LeadCreator` no construtor |
| cmd/api/main.go | Criar funnel module, passar lead creator ao whatsapp module |
| web/templates/partials/tenant_sidebar.html | Habilitar link "Leads" |

---

## 8. Steps de Implementacao

### Step 1: Dominio + migrations
**Escopo**: entidades, erros, interfaces de repositorio, migrations, testes unitarios.

**Arquivos criados:**
- `internal/funnel/domain/funnel.go` + test
- `internal/funnel/domain/column.go` + test
- `internal/funnel/domain/lead.go` + test
- `internal/funnel/domain/lead_movement.go`
- `internal/funnel/domain/defaults.go`
- `internal/funnel/domain/errors.go`
- `internal/funnel/domain/repository.go`
- `migrations/000019-000022`

---

### Step 2: Repositorios Gorm
**Escopo**: implementacao dos repos, testes de integracao.

**Arquivos criados:**
- `internal/funnel/infrastructure/models.go`
- `internal/funnel/infrastructure/gorm_funnel_repository.go` + test
- `internal/funnel/infrastructure/gorm_column_repository.go` + test
- `internal/funnel/infrastructure/gorm_lead_repository.go` + test
- `internal/funnel/infrastructure/gorm_lead_movement_repository.go` + test

---

### Step 3: Casos de uso (funis e colunas)
**Escopo**: CRUD funil, CRUD colunas, colunas padrao, testes unitarios.

**Arquivos criados:**
- `internal/funnel/application/create_funnel.go` + test
- `internal/funnel/application/list_funnels.go` + test
- `internal/funnel/application/get_funnel.go` + test
- `internal/funnel/application/update_funnel.go` + test
- `internal/funnel/application/toggle_funnel.go` + test
- `internal/funnel/application/create_column.go` + test
- `internal/funnel/application/delete_column.go` + test
- `internal/funnel/application/move_column.go` + test
- `internal/funnel/application/mocks_test.go`

---

### Step 4: Casos de uso (leads e kanban)
**Escopo**: criar lead, mover lead, detalhe, kanban, testes unitarios.

**Arquivos criados:**
- `internal/funnel/application/create_lead.go` + test
- `internal/funnel/application/move_lead.go` + test
- `internal/funnel/application/get_lead_detail.go` + test
- `internal/funnel/application/get_kanban.go` + test

---

### Step 5: Integracao com WhatsApp
**Escopo**: interface LeadCreator, atualizar ReceiveMessageUseCase, wire-up.

**Arquivos criados/alterados:**
- `internal/whatsapp/domain/lead_creator.go` (nova interface)
- `internal/whatsapp/application/receive_message.go` (adicionar LeadCreator)
- `internal/whatsapp/module.go` (receber LeadCreator)
- `cmd/api/main.go` (wire-up funnel → whatsapp)

---

### Step 6: Handlers HTTP + rotas
**Escopo**: handlers para funis, colunas, leads, kanban. Retornam JSON temporariamente.

**Arquivos criados:**
- `internal/funnel/interfaces/http/handler.go`
- `internal/funnel/interfaces/http/funnel_handler.go`
- `internal/funnel/interfaces/http/column_handler.go`
- `internal/funnel/interfaces/http/lead_handler.go`
- `internal/funnel/interfaces/http/routes.go`
- `internal/funnel/interfaces/http/handler_test.go`
- `internal/funnel/module.go`

---

### Step 7: Templates + CSS
**Escopo**: kanban, gestao de funis, detalhe do lead, Sortable.js.

**Arquivos criados:**
- `web/templates/funnel/kanban.html`
- `web/templates/funnel/kanban_column.html`
- `web/templates/funnel/lead_card.html`
- `web/templates/funnel/lead_detail.html`
- `web/templates/funnel/lead_move.html`
- `web/templates/funnel/funnel_list.html`
- `web/templates/funnel/funnel_detail.html`
- `web/templates/funnel/funnel_form.html`
- `web/templates/funnel/column_form.html`
- `web/templates/funnel/columns_section.html`
- `web/static/css/kanban.css`
- `web/static/js/kanban.js` (Sortable.js integration)

**Arquivos alterados:**
- `web/templates/partials/tenant_sidebar.html` (habilitar Leads)

---

### Step 8: Observabilidade + testes OWASP + arquivos .http

**Arquivos criados/alterados:**
- Metricas Prometheus: leads criados, movidos
- Testes OWASP: 401/403, tenant isolation, injection
- `rest/06-funis-kanban.http` (atualizar)
